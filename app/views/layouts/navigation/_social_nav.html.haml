
-# THE NAVBAR

.list-group
  %a.list-group-item.list-group-item-success{href: main_app.social_path, class: "#{"active" if action_name == "overview"}"} Map & Stats
  - if myapnea_pprn? && current_user
    %a.list-group-item.list-group-item-success{href: forem.root_path, class: "#{"active" if action_name == "discussion"}"} Discussion Forum
  %a.list-group-item.list-group-item-success{href: main_app.research_topics_path, class: "#{"active" if action_name == "research_topics"}"} Research for Vote
  - if current_user
    %a.list-group-item.list-group-item-success{href: main_app.karma_path, class: "#{"active" if action_name == "karma"}"} My Karma
  %a.list-group-item.list-group-item-success{href: main_app.blog_path, class: "#{"active" if action_name == "blog"}"} Blog



-# Description of this section
.small
  %p Patients are on #{@pprn_title} from all around the world. Fusce erat velit, luctus at tempor non, hendrerit sit amet ligula. Praesent quis eleifend magna. Proin nec nibh ut mi elementum tristique sit amet non est.

- if current_user
  %a.btn.btn-success.btn-block{href: main_app.social_profile_path, class: "#{"active" if action_name == "profile"}"} Update Social Profile
  %br/

= render partial: "shared/promotion_to_join" if !current_user


- if current_user
  .panel.panel-default
    .panel-heading
      %h3.panel-title Around You

    %ul.list-group
      - 6.times do
        %li.list-group-item
          .media
            .pull-left
              = image_tag "http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(rand.to_s)}?d=identicon", :class => " media-item-object img-circle user-avatar"
            .media-body.small
              %strong
                = random_name
              %p
                = random_place




