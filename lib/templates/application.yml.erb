# Add configuration values here, as shown below.
#
# pusher_app_id: "2954"
# pusher_key: 7381a978f7dd7f9a1117
# pusher_secret: abdc3b896a0ffb85d373
# stripe_api_key: sk_test_2J0l093xOyW72XUYJHE4Dv2r
# stripe_publishable_key: pk_test_ro9jV5SNwGb1yYlQfzG17LHK
#
# production:
#   stripe_api_key: sk_live_EeHnL644i6zo4Iyq4v1KdV9H
#   stripe_publishable_key: pk_live_9lcthxpSIHbGwmdO941O1XVU


#####################
# The title of your network and the conditions it helps
#####################
website_code_name:         "<%= print "Enter Website Code Name (Should Contain No Spaces). This is used as the theme directory name: "; ga = gets.chomp; ga %>"

website_url:               "<%= print "Enter Website URL: "; gets.chomp.gsub(/(\/)*$/, '') %>"
website_title:             "<%= print "Enter Website Name/Title: "; gets.chomp %>"

website_support_email:     "<%= print "Enter support email address: "; ga = gets.chomp; ga %>"
website_info_email:        "<%= print "Enter info email address: "; ga = gets.chomp; ga %>"
website_condition:         "<%= print "PPRN Conditions: "; ga = gets.chomp; ga %>"
website_conditions:        <%= ga.split(" ") %>



#####################
# Application Behavior Configuration
#####################
# Voting on Research Topics
votes_per_user:         "<%= print "Enter the number of votes each user has to spend on Research Topics "; ga = gets.chomp; ga %>"

# Health Check ins
check_in_frequency:     "<%= print "Enable frequency for your user's health check ins (hours):"; ga = gets.chomp; ga %>"



#####################
# Third Party Services / APIs
#####################
uservoice_id:             "<%= print "Enter uservoice ID: "; gets.chomp %>"
google_analytics_id:      "<%= print "Google Analytics ID: "; gets.chomp %>"

# Facebook Integration
fb_app_id:                "<%= print "Enter the facebook app ID: "; gets.chomp %>"
fb_app_secret:            "<%= print "Enter the facebook app secret: "; gets.chomp %>"

validic_organization_id:  "<%= print "Enter validic organization id: (leave blank if not using OODT)"; gets.chomp %>"
validic_access_token:     "<%= print "Enter validic access token: (leave blank if not using OODT)"; gets.chomp %>"

oodt_username:            "<%= print "Enter OODT username (leave blank if not using OODT): "; gets.chomp %>"
oodt_password:            "<%= print "Enter OODT password (leave blank if not using OODT): "; gets.chomp %>"





#####################
# SMTP Email Setup
#####################
smtp_address:         "<%= print "Enter SMTP Address [localhost]: "; s = gets.chomp; s == '' ? 'localhost' : s %>"
smtp_port:            "<%= print "Enter SMTP Port [25]: "; s = gets.chomp; s == '' ? '25' : s %>"
smtp_authentication:  "<%= print "Enter SMTP Authentication [plain]: "; s = gets.chomp; s == '' ? 'plain' : s %>"
smtp_email:           "<%= print "Enter SMTP Email Address: "; gets.chomp %>"
smtp_user_name:       "<%= print "Enter SMTP User Name [username]: "; s = gets.chomp.gsub('"', '\"').gsub("\\", "\\\\"); s == '' ? 'username' : s %>"
smtp_password:        "<%= print "Enter SMTP Password [password]: "; s = gets.chomp.gsub('"', '\"').gsub("\\", "\\\\"); s == '' ? 'password' : s %>"



#####################
# Database URL
#####################
database_url:         "<%= print "Enter Database URL: "; gets.chomp %>"




#####################
# Secret Key
#####################
# For this to work, the following line needs to be in your secrets.yml file:
# production:
#  secret_key_base: <%%= ENV["SECRET_KEY_BASE"] %>
secret_key_base:      "<%= print "Enter your secret key base (run 'rake secret' in another window. Put the result here)"; gets.chomp %>"
secret_key:           "<%= print "Enter your secret key (run 'rake secret' in another window. Put the result here)"; gets.chomp %>"
